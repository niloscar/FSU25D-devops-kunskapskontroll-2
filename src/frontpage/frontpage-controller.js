import { renderFrontpage } from './frontpage-view.js';

export function initFrontpage() {
    // Ladda funktioner etc som vi vill ha på startsidan här.

    renderFrontpage();
    console.log('Frontpage initialized');
}